<!DOCTYPE html>
<html lang="en">
{{> shared/head}}
<body id="{{slug}}">
  <section id="wrapper">
    {{> shared/header}}
      <nav id="sub-navigation">
	    <ol>
		{{# children}}
		  <li><a href="{{permalink}}">{{name}}</a></li>
		{{/ children}}
		</ol>
      </nav>

	  <article>
	  	<h2>{{headline}}</h2>
        {{{body}}}
	  </article>
	
    {{> shared/footer}}
  </section>
</body>
{{> shared/analytics }}
</html>